import fs from 'node:fs';import path from 'node:path';
const title=process.argv.slice(2).join(' ').trim();if(!title){console.error('Usage: npm run new:article -- "Article title"');process.exit(1)}const slug=title.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');const file=path.join('src','content','insights',`${slug}.md`);if(fs.existsSync(file)){console.error('Article already exists:',file);process.exit(1)}const today=new Date().toISOString().slice(0,10);const body=`---\ntitle: "${title.replaceAll('"','\\"')}"\ndescription: "Add a concise 140–160 character summary."\npublishDate: ${today}\ncategory: "Consumer Intelligence"\ntags: []\nfeatured: false\ndraft: true\n---\n\nWrite the article here. Set draft to false when approved.\n`;fs.writeFileSync(file,body);console.log(file);
